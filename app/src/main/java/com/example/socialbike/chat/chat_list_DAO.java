package com.example.socialbike.chat;
/*

import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.Query;
import java.util.List;

@Dao
public interface chat_list_DAO {

    @Query("SELECT * FROM chat_list")
    List<User> getAll();

    @Query("SELECT * FROM chat_list WHERE sender_id = (:userId)")
    List<User> loadAllByIds(int userId);

*/
/*    @Query("SELECT * FROM chat_list WHERE first_name LIKE :first AND " +
            "last_name LIKE :last LIMIT 1")
    User findByName(String first, String last);*//*


    @Insert
    void insert(User user);

    @Delete
    void delete(User user);
}*/
